node_modules
.next
out
.env
.env*.local
.DS_Store
*.tsbuildinfo
next-env.d.ts
.vercel
supabase/.branches
supabase/.temp

# Private / confidential — never commit these
AGREMTS/
New folder/
*recovery-codes*
*credentials*
*.pem
*.key

# Capacitor native builds — the android/ and ios/ project files are
# committed, but build output and local signing material are not.
android/app/build
android/.gradle
android/local.properties
android/*.iml
android/.idea
ios/App/Pods
ios/App/build
ios/App/DerivedData
*.keystore
*.jks
*.p12
*.mobileprovision
