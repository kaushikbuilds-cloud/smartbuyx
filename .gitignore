node_modules
.next
out
.env
.env*.local
.DS_Store
*.tsbuildinfo
next-env.d.ts
.vercel
supabase/.branches
supabase/.temp

# Private / confidential — never commit these
AGREMTS/
New folder/
*recovery-codes*
*credentials*
*.pem
*.key

# Security audit reports/plans — contain exploit-level detail; keep local until
# every finding's fix is confirmed applied in production, then share deliberately.
security/
